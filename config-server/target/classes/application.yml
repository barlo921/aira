spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/barlo921/aira/services-configs
          username: barlo@list.ru
          password: A11YouNeedIsLove
          clone-on-start: true
          default-label: main

server:
  port: 8888